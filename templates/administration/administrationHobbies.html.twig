{% extends 'base.html.twig' %}

{% block title %}CV Guillaume Vignères - Administration{% endblock %}

{% block body %}
    <main>
        <section class="containerSectionAdmin">
            {% include('/administration/navAdministration.html.twig') %}
            <div class="containerAdmin">
                
                <div class="adminTab">
                <h3>Hobbies<a href="{{ path('addHobby')}}"><img src="{{ asset('img/icons8-plus-50.png') }}"></a></h3>
                
                    <table>
                        <thead>
                            <tr>
                                <th class="column1">Hobby</th>
                                <th class="column3">Modifier</th>
                                <th class="column4 columnEnd">Supprimer</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for hobby in hobbies %}
                                <tr>
                                    <td class="column1">{{ hobby.hobbiesName }}</td>
                                    <td class="column3"><a href="{{ path('modifyHobby', {'id': hobby.id}) }}"><img src="{{ asset('img/crayon.png') }}"></a></td>
                                    <td class="column4 columnEnd"><a href="{{ path('deleteHobby', {'id': hobby.id}) }}?admin=1" class="delete" data-confirm="Êtes vous sûr de vouloir effacer ce projet?"><img src="{{ asset('img/poubelle.png') }}"></a></td>                                    
                                </tr>
                            {% endfor %}
                        </tbody>
                    </table>   
                </div>
                <div class="containerAdminMobile">
                <h3>Hobbies<a href="{{ path('addHobby')}}"><img src="{{ asset('img/icons8-plus-50.png') }}"></a></h3>
                    {% for hobby in hobbies %}
                        <table class="panel">
                            <tr class="trCompte">
                                <td>Hobby:</td>
                                <td>{{ hobby.hobbiesName }}</td>
                            </tr>
                            <tr class="trCompte">
                                <td>Modifier:</td>
                                <td><a href="{{ path('modifyHobby', {'id': hobby.id }) }}"><img src="{{ asset('img/crayon.png') }}"></a></td>
                            </tr>
                            <tr class="trCompte">
                                <td>Supprimer:</td>
                                <td><a href="{{ path('deleteHobby', {'id': hobby.id }) }}?admin=1" class="delete" data-confirm="Êtes vous sûr de vouloir effacer ce projet?"><img src="{{ asset('img/poubelle.png') }}"></a></td>                                    
                            </tr>
                        </table>
                    {% endfor %}
                </div>
                <div id="pagingAdminUsers" class="paging border-top">
                    {% include('/paging/paging.html.twig') %}
                </div>  
            </div>
        </section>
    </main>
{% endblock %}
